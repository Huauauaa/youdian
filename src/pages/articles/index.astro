---
import { articleSections, articles } from "../../data/articles";
import "../../styles/global.css";

const base = import.meta.env.BASE_URL;
const pageBackgroundClass =
	"min-h-screen bg-[radial-gradient(circle_at_top_left,rgba(180,83,9,0.1),transparent_30rem),linear-gradient(180deg,#fffaf0_0%,#f8f1e3_100%)] font-sans text-youdian-ink";
const eyebrowClass =
	"mb-4 text-[0.78rem] font-extrabold uppercase tracking-[0.2em] text-youdian-accent";
const articleCardClass =
	"glass-card grid gap-[0.8rem] rounded-[1.4rem] p-5 text-inherit no-underline";
---

<html lang="zh-CN">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
		<link rel="icon" href={`${base}favicon.ico`} />
		<meta name="viewport" content="width=device-width" />
		<meta
			name="description"
			content="游殿村故事的文章目录，整理已经写好的村庄故事。"
		/>
		<meta name="generator" content={Astro.generator} />
		<title>文章目录 | 游殿村故事</title>
	</head>
	<body class={pageBackgroundClass}>
		<main class="mx-auto box-border w-full max-w-[62rem] px-5 pt-7 pb-16">
			<nav
				class="mb-10 flex flex-wrap items-center justify-between gap-4 rounded-full border border-youdian-ink/10 bg-white/55 px-4 py-3 shadow-[0_0.75rem_2rem_rgba(23,32,51,0.05)] backdrop-blur"
				aria-label="目录导航"
			>
				<a
					class="font-bold text-youdian-accent no-underline transition hover:text-youdian-rust focus-visible:text-youdian-rust"
					href={base}
				>
					← 返回首页
				</a>
				<span class="rounded-full bg-orange-50 px-3 py-1 text-sm font-bold text-youdian-rust">{articles.length} 篇文章</span>
			</nav>

			<header class="mb-10 rounded-[2rem] border border-youdian-ink/10 bg-white/70 p-[clamp(1.4rem,5vw,3rem)] shadow-[0_1.5rem_4rem_rgba(23,32,51,0.08)]">
				<p class={eyebrowClass}>Article Directory</p>
				<h1 class="m-0 text-[clamp(2.4rem,8vw,4.6rem)] font-extrabold leading-[1.08] tracking-[-0.04em]">游殿村故事目录</h1>
				<p class="max-w-[34rem] text-[1.05rem] leading-[1.8] text-youdian-muted">
					这里只放已经写好的文章。列表保持克制，方便直接进入阅读。
				</p>
			</header>

			<div class="grid gap-4">
				{articleSections.map((section) => (
					<section
						class="grid gap-5 rounded-[1.6rem] border border-youdian-ink/10 bg-white/60 p-5 shadow-[0_1rem_3rem_rgba(23,32,51,0.06)] md:grid-cols-[minmax(10rem,15rem)_1fr]"
						aria-labelledby={`catalog-${section.title}`}
					>
						<div>
							<h2 id={`catalog-${section.title}`} class="m-0 text-2xl leading-[1.1]">
								{section.title}
							</h2>
							<p class="text-[1.05rem] leading-[1.8] text-youdian-muted">
								{section.description}
							</p>
						</div>
						<div class="grid gap-4 lg:grid-cols-2">
							{section.articles.map((article) => (
								<a class={articleCardClass} href={`${base}${article.href}`}>
									<div class="flex items-center justify-between gap-3">
										<span class={`${eyebrowClass} m-0`}>{article.category}</span>
										<span class="text-lg text-youdian-accent">→</span>
									</div>
									<strong class="text-[1.25rem] leading-[1.25]">{article.title}</strong>
									<small class="text-[0.95rem] leading-[1.65] text-youdian-muted">
										{article.description}
									</small>
									<em class="status-badge not-italic">
										{article.status}
									</em>
								</a>
							))}
						</div>
					</section>
				))}
			</div>
		</main>
	</body>
</html>
